import React from 'react';
import { render } from 'react-dom';

import App from './App';

// renderiza um componente react dentro de um componente html
render(<App />, document.getElementById('app'));
